# Netdata Monitoring Server

Environment
Proxmox VM running Ubuntu Server

IP Address
10.0.0.164

Installation

sudo apt update
sudo apt install curl
bash <(curl -L https://my-netdata.io/kickstart.sh)

Result
Netdata monitoring dashboard deployed.

Access URL
http://10.0.0.164:19999
